# Проект небесный архив

### 🔹 Рекомендуемая конфигурация

- PHP 8.1 (для php требуются следующие библиотеки)
    - php8.1-gd
    - php8.1-xml
    - php8.1-mysqli
    - php8.1-mbstring
- MySQL 5.7
- Symfony 6.4.8 (от 02.06.24)
- HTTP/2
- требования к железу: не исследовано...

### 🔹 Разворачивание веб-приложения

1. Подключиться к данному репозиторию и клонировать его.
2. Из резервной копии взять следующие каталоги с содержимым (т.к. хранить это в репозитории нецелесообразно):
- ../node_modules
- ../public/img
3. Создать файлы конфигурации (в зависимости от типа приложения prod или dev) из wiki проекта

### 🔹 Общая информация

Проект передавался от предыдущего разработчика(ов) и имел следующие особенности:
- повреждённая бд, 
- нерабочий поиск внутри приложения по qr-кодам,
- полное отсутствие документации и инструкций,
- 3 коммита за всё время разработки с 0,
- ошибки при сборке webpack

##### ◻ Документация

- Symfony (оффициальная): https://symfony.com/doc/current/index.html
- Symfony (русская): https://symfony.ru/doc/current/index.html

##### ◻ Поиск

- `./src/Controller/Admin/QrCode/QrCodeTableController.php`
- `./src/Handler/DataTables/BaseDataTable.php`

##### ◻ Работа с БД

1. Используется решение DataTables: https://github.com/webinarium/DataTablesBundle
2. Используется концепция ORM: https://github.com/doctrine/orm/tree/3.0.x
3. Используется DQL (Doctrine Query Language):
    - https://symfony.com/doc/current/doctrine.html
    - https://www.doctrine-project.org/projects/doctrine1/en/latest/manual/dql-doctrine-query-language.html (через VPN)

##### ◻ Работа с js, css и прочими ресурсами

- Используется Symfony Encore: https://symfony.ru/doc/current/frontend/encore/simple-example.html

Общий принцип такой:
1. Ресурсы собираются в катлоге `./assets`
2. При помощи webpack они билдятся в `./public/build`
3. Также в каталоге `./public` добавленены дополнительные ресурсы, которые использует приложение, и которые не учавствуют в сборке.
4. Сборку проекта осуществлять следующими командами:

```bash
npm i --force
npm run build
```

5. В ходе сборки присутствует большое количество предупреждений (warning) и в конце сборка завершается ошибкой.

##### ◻ Переводы

- файл с переводом находится здесь: `./translations/messages.ru.xlf`

##### ◻ Шаблоны

- располагаются здесь: `./templates`

### ❌ Обнаруженные баги в проекте

1. Не работает сборка webpack.
2. Не работает поиск внутри приложения по qr-кодам. В текущий момент поиск работает по полям **ID** и **Уникальный номер**.
3. Нужно изучать ситуацию, когда администратор редактирует заказ клиента. Возможны баги и перезаписывание полей таблицы на неверные.
4. \[[- CRITICAL -]\] Баг с функционалом редактирования qr-кода (когда меняем label у qr-кода). Работает нестабильно и приводит к порче всей бд (данные qr-кодов подменяются данными пользователя с id = 16). Был закомментриван в файле
```../templates/admin/qr-code/_datatables_index_actions.html.twig (16-19).```# MemoryProject
